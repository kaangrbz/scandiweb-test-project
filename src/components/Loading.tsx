import '../styles/_loading.scss';

const Loading = () => {
  return (
    <div className="container">
      <div className="loading">Loading the page, please wait</div>
    </div>
  );
};

export default Loading;
